# Лабораторна робота №4-5

## Навчальний заклад

Відокремлений структурний підрозділ
**"Фаховий коледж інформаційних технологій**
**Національного університету “Львівська політехніка”**

**Виконав:** студент групи МТ-22
**Одарчук Олексій**

---

## Тема

>Алгоритми з повторенням мови Python. Цикли з умовою

## Мета

>Навчитись розробляти циклічні алгоритми та програми їх реалізації виконувати налагодження програм

## Завдання

1. Скласти блок-схеми та програми на мові програмування Python. При виконанні даного завдання обчислити вираз з використанням операторів циклу з **передумовою** та **післяумовою**

    **Передумова:**

    ```mermaid
    flowchart TD
        A[Початок] --> B{Ініціалізація: P=1, k=3};
        B --> C{k <= 12};
        C -- Так --> D["P = P * k^2 * (1 + log(k))"];
        D --> E[k = k + 1];
        E --> C;
        C -- Ні --> F[Повернути P];
        F --> G[Кінець];
    ```

    **Післяумова:**

    ```mermaid
    flowchart TD
        A["Початок"] --> B{"Ініціалізація: P=1, k=3"};
        B --> C["P = P * k^2 * (1 + log(k))"];
        C --> D["k = k + 1"];
        D --> E{"k > 12"};
        E -- Ні --> C;
        E -- Так --> F["Повернути P"];
        F --> G["Кінець"];
    ```

2. Скласти блок-схему та програму з використанням **циклу з післяумовою**: Список містить числа 2, 5, 7, 9, 15. З’ясувати, на якій позиції знаходиться число 7

    ```mermaid
    flowchart TD
        A["Початок"] --> B{"Ініціалізація: list=[...], index=0"};
        B --> C{"Цикл While True"};
        C --> D{"list[index] == 7"};
        D -- Так --> E["Зупинити цикл (break)"];
        E --> J;
        D -- Ні --> F["index = index + 1"];
        F --> G{"index == len(list)"};
        G -- Так --> H["index = -1"];
        H --> I["Зупинити цикл (break)"];
        I --> J;
        G -- Ні --> C;
        J["Вивід результату"];
        J --> K["Кінець"];
    ```

3. Скласти блок-схему та програму з використанням **циклу з післяумовою**: Генерується послідовність цілих чисел у діапазоні від 3 до 40 з кроком 3. Вивести на екран усі числа, крім чисел у діапазоні від 14 до 31.

    ```mermaid
    flowchart TD
            A["Початок"] --> B{"Створення кортежу: custom_tuple (числа % 3 == 0, 3..40)"};
            B --> C{"Ініціалізація: index=0"};
            C --> D{"Цикл While True"};
            D --> E{"index == len(custom_tuple)"};
            E -- Так --> G["Зупинити цикл (break)"];
            E -- Ні --> H["index = index + 1"]; 
            H --> Z{"14 < custom_tuple[index] < 31"};
            Z -- Так --> I["Повернутися до початку циклу (continue)"];
            Z -- Ні --> F['Вивести результат'];
            I --> D;
            F --> D;
            G --> K["Кінець"];
    ```

4. Скласти блок-схему та програму з використанням циклу з передумовою: З клавіатури вводиться ціле десяткове число довільної розрядності. Визначити суму цифр цього числа.

   ```mermaid
    flowchart TD
        A["Початок"] --> B["Введення числа (як рядка) 'number'"];
        B --> C{"Ініціалізація: index=0, S=0"};
        C --> D{"len(number) > index"};
        D -- Так --> E["S = S + int(number[index])"];
        E --> F["index = index + 1"];
        F --> D;
        D -- Ні --> G["Вивід суми S"];
        G --> H["Кінець"];
    ```

## Відповіді на контрольні запитання

## 💻 Види та Оператори Циклічних Алгоритмів

Ось відповіді на ваші запитання щодо циклічних алгоритмів та їхніх операторів:

---

1. **Які існують види циклічних алгоритмів?**

    В Python існує два основні механізми:

    * **Цикл з передумовою (while):** Виконує блок коду, поки умова є **істинною**.
    * **Цикл із лічильником (for):** Виконує ітерацію через елементи **послідовності** (список, діапазон, рядок тощо).
    * **Цикл з післяумовою:** в Python немає нативної реалізації цього циклу, але його логіка легко імітується за допомогою **нескінченного циклу** та **`break`**:

    ---

2. **Яку структуру має оператор циклу з передумовою?**

    Оператор циклу з передумовою (`while`):

    ```python
    counter = 0 

    while counter < 5:  # Умова
        # Тіло циклу
        print(counter)
        counter += 1 
    ```

    ---

3. **Яку структуру має оператор циклу з післяумовою?**

    В Python **немає** прямого оператора циклу з післяумовою. Його логіка імітується за допомогою **нескінченного циклу** та **`break`**:

    ```python
    while True:
        # Тіло циклу виконується хоча б один раз
        answ = input("Введіть 'exit' для виходу: ")

        if answ == "exit":
            break # Вихід, якщо умова виконана
    ```

    ---

4. **Для чого призначений оператор `break`?**

    Оператор **`break`** призначений для **негайного виходу** (дострокового завершення) з найближчого циклу (`for` або `while`).

    * **Призначення:** Повністю **припиняє** виконання циклу.

    ---

5. **Для чого призначений оператор `continue`?**

    Оператор **`continue`** призначений для **пропуску поточної ітерації** циклу та негайного переходу до наступної ітерації.

    * **Призначення:** Ігнорує решту коду в тілі циклу для поточної ітерації.
